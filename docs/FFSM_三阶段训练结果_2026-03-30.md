# FFSM 三阶段训练结果记录（2026-03-30）

## 1. 本次运行目标
在 FFSM 环境上串联验证三阶段流程：
- 阶段1：`rl-zoo3` 训练 SAC 专家；
- 阶段2：用现有数据预训练 ReFlow；
- 阶段3：基于预训练策略做 PPO-ReFlow 微调。

## 2. 本次主要配置
- 训练脚本：`script/start_ffsm_three_stage_tmux_test.sh`
- 关键参数（启动时）：
  - `CUDA_EXPERT=1 CUDA_PRETRAIN=2 CUDA_FINETUNE=3`
  - `EXPERT_TIMESTEPS=8000000`
  - `PRETRAIN_EPOCHS=200`
  - `FINETUNE_ITR=1000 FINETUNE_STEPS=256 FINETUNE_BATCH_SIZE=4096`
- finetune 首次失败后，改为 `env.n_envs=8` 重启（避免 `logprob_batch_size` 整除断言）。

## 3. 阶段结果概览

### 3.1 专家策略（SAC）
- 产物目录（本次历史运行）：`../FFSM_Env/rl-zoo3/logs/sac/FFSMEnv6dof-v0_10`
- 当前分支统一目录（改造后）：`log/expert/rl_zoo3/sac/FFSMEnv6dof-v0_*`
- 有效评估点：`timesteps=[24996, 49992]`
- 评估均值回报：
  - 24,996 步：`-172.28`
  - 49,992 步：`-191.39`
- 关键文件：
  - `best_model.zip`
  - `evaluations.npz`

### 3.2 ReFlow 预训练
- 产物目录：`log/gym/pretrain/FFSMEnv6dof-v0_pre_reflow_mlp_ta1_td4_seed42/2026-03-30_15-24-04_42`
- 最新日志记录到：
  - `epoch=21/200`
  - `train loss=0.1933`
  - 最新 eval：`success_rate=0.375`，`avg_episode_reward=-61.4±22.0`
- checkpoint：`checkpoint/last.pt`

### 3.3 PPO-ReFlow 微调
- 产物目录：`log/gym/finetune/FFSMEnv6dof-v0_ppo_reflow_mlp_ta1_td4_tdf4/2026-03-30_15-29-16_seed42`
- 有效 eval 记录数：`26`（`itr=0..50` 每 2 次评估）
- 最佳 eval（按 `eval/avg episode reward`）：
  - `itr=34`
  - `eval_reward=-53.96`
  - `eval_success_rate=0.50`
- 最新 eval（`itr=50`）：
  - `eval_reward=-76.74`
  - `eval_success_rate=0.25`
- 关键 checkpoint：
  - `checkpoint/best.pt`
  - `checkpoint/last.pt`

## 4. 本轮发现的问题
- finetune 首次启动失败：`logprob_batch_size must be divisible by n_envs`。
- 已在启动脚本中固化防护：
  - 新增 `FINETUNE_LOGPROB_BATCH_SIZE`；
  - 自动对齐到可被 `FINETUNE_N_ENVS` 整除（或 strict 模式直接报错）。
- 对应修复提交：`e550672`。

## 5. 快速查看入口
- 三阶段 tmux 总日志：`log/ffsm_tmux/`
- pretrain 运行日志：`.../2026-03-30_15-24-04_42/run.log`
- finetune 运行日志：`.../2026-03-30_15-29-16_seed42/run.log`
- 离线 wandb：`wandb_offline/wandb/offline-run-20260330_*`
